error: agent authority: `post(...)` is handed a URL the analysis cannot read, so the hosts `call_out` can reach cannot be proven — a `format!`-built URL takes its host from runtime data.

       Pass an absolute literal URL, use a configured client alias (`named("...")`), or declare the host with `#[agent_effect(outbound("https://..."), reason = "...")]`. See docs/guide/agent-authority.md.
  --> tests/compile-fail/agent_authority_outbound_dynamic_url.rs:33:12
   |
33 |     client.post(&url).send().await
   |            ^^^^
