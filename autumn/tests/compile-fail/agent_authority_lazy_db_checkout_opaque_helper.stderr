error: agent authority: `finalize` is handed the effect handle `db`, and what it does with it is another function's business — so `draft`'s effects cannot be proven.

       Move the effect into this handler, declare what the call does with `#[agent_effect(writes(Model), reason = "...")]`, or — if it is verified to perform none — discharge it with `#[agent_effect(none, reason = "...")]`. See docs/guide/agent-authority.md.
  --> tests/compile-fail/agent_authority_lazy_db_checkout_opaque_helper.rs:40:5
   |
40 |     finalize(&mut db, 1).await
   |     ^^^^^^^^
